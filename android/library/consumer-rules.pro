# R8/ProGuard rules consumed by apps that depend on this .aar.
# We don't yet need any specific keep rules since UniFFI's
# generated Kotlin doesn't use reflection-driven patterns. If
# we add anything later that needs reflection, add it here.
